# Graphus — Functional Specification

This directory is the single source of truth for the **functional specification** of
**Graphus**, a Label Property Graph (LPG) database server written in Rust.

The specification is built **Specify → Implement → Test → Document**. This baseline
covers the **Specify** stage: a global survey of every need derived from the project
definition, the non-functional/quality requirements, the open design decisions, and the
phased scope. It is grounded in authoritative sources (see `03-sources.md`) and mirrored
in the project Knowledge Graph (`rmp` roadmap `graphus`).

## Documents

| File | Purpose |
| --- | --- |
| `00-overview.md` | Project definition, goals, scope boundaries, glossary, non-functional requirements, phased roadmap, traceability. |
| `01-needs-survey.md` | **The global needs survey** — every functional need, organized by the 15 domains, each tagged `[CORE]` or `[ADV]`. |
| `02-decision-register.md` | **49 design decisions**, enumerated canonically — one row per decision, with its key, status, ratification date, and ratified choice — in the "Decision index (canonical)" section, which is the only place to read the decision list from; the "Options and rationale" section keeps the options weighed and the recommendation made for each. The 24 baseline decisions were **all ratified on 2026-06-05**; the other twenty-five carry their own later dates. The chosen option is recorded on each KG `Decision` node. Tracks the open questions and which spikes close them. Also records two post-ratification sprint-19 deferrals (`D-read-parallelism`, rmp #146; `D-perf-deferrals`, rmp #159), the design decision `D-named-index-autoname` (named node-property indexes, rmp #623–#626; a post-ratification note completing the ratified `FR-IX-15` DDL — see `04-technical-design.md` §6.8), and two post-ratification decisions: `D-bulk-import-network`, **ratified 2026-07-01** (global `Admin` RBAC + both a fresh/empty-database and an already-live-database import mode — see the "Ratified decision (2026-07-01)" section), and `D-hw-autotune`, **ratified 2026-07-08** (hardware-aware startup auto-tuning: the `0 = auto` sentinel + operator-config-wins precedence, the conservative per-database buffer-pool sizing, and the `graphus-sysres` probe leaf crate — see the "Ratified decision (2026-07-08)" section; design in `04-technical-design.md` §9.5), and the post-ratification design decision `D-query-prefixes` (the `EXPLAIN`/`PROFILE` query prefixes, rmp #752: `EXPLAIN` plans without executing; `PROFILE` executes and reports each operator's measured `rows`/`dbHits`; timing and page-cache counters are omitted rather than fabricated — see the "Post-ratification note (2026-07-14)"; design in `04-technical-design.md` §7.8), and the four **MVCC-native engine** decisions **ratified 2026-08-02** (`D-version-representation`, `D-write-conflict-detection`, `D-multi-writer`, `D-dst-writer-scheduler`: newest version in place with one unified undo-delta chain per entity; write-write conflicts detected on the MVCC header and aborted without waiting, retiring the lock table and the deadlock detector; N writers in parallel; and a deterministic DST writer scheduler as the prerequisite of multi-writer certification — see the "Ratified decision (2026-08-02)" section; design in `04-technical-design.md` §§5.1, 5.6, 5.7 and `05-storage-format.md` §12), and the four **property-path** decisions **ratified 2026-08-03** (`D-property-write-conflict`, `D-property-removal`, `D-property-visibility`, `D-retired-mechanism-tests`: the entity-granularity write-write conflict check arrives with task #967; a property removal is an empty cell in place rather than an `expired_ts` tombstone, with exactly one owner per overflow chain; the undo chain is the sole visibility oracle for a property's value, so the cell's `created_ts` is informative only; and "all existing tests stay green" is read as "every semantic those tests protected remains asserted by a test that fails if the semantic breaks" — see the "Ratified decision (2026-08-03)" section; design in `04-technical-design.md` §§5.1.2, 5.1.5, 5.6, 11.6 and `05-storage-format.md` §§7, 12.2), plus `D-incidence-anchor`, **ratified 2026-08-04** (an incidence delta anchors on the relationship, not on the endpoint node, so a hub's chain does not grow with its degree; design in `04-technical-design.md` §5.1.1), and the three **logical-rollback** decisions **ratified 2026-08-05** (`D-rollback-dispatch`, `D-chain-head-redo-only`, `D-orphan-slot-parking`: a rollback is logical when the transaction owns a commit-info slot and physical when it does not; chain-head writes carry no undo image, because their inverse is to unlink the entry; and a slot orphaned by a logical rollback is parked until the next GC pass, because the in-memory text and spatial indexes key their documents by physical node id — see the "Ratified decision (2026-08-05) — logical rollback" section; design in `04-technical-design.md` §§4.3, 4.4, 5.1.2, 5.1.5 and `05-storage-format.md` §§7, 9, 12.3, 12.5), and `D-statement-isolation`, also **ratified 2026-08-05** (statement-level isolation over the delta's `command_id`: every read carries a `New`/`Old` view beside its snapshot that decides only whether the reader's **own** uncommitted deltas are undone; `command_id = 0` marks a write made outside any statement and no view undoes it; the counter lives in the record store and advances at exactly two points — cursor open, and a `WITH` that follows a write; polarity is fixed per clause, `Old` for every access path and `New` for every update clause and projection; and the planner's `Eager` barriers are all retained — see the "Ratified decision (2026-08-05) — statement-level isolation" section; design in `04-technical-design.md` §§5.1.4, 5.3 and `05-storage-format.md` §12.2), and `D-chain-head-publication`, **ratified 2026-08-09** (how a chain head — `first_rel`, `first_prop`, `undo_ptr` — is published safely once more than one writer may prepend to it, rmp #1028: a compare-and-publish against the head the entry was linked to, with the displaced predecessor's back-pointer fixed only after a winning publication; the protocol in the dependency-free leaf crate `graphus-chainhead` so `loom` can model-check it; a sharded rank-27 publication latch supplying both atomicity and durable log order; a refused publication logging nothing at all; a conditional redo image that needs no format change; and the same latch covering the shared `chain_flags` byte and the GC corpse splice — see the "Ratified decision (2026-08-09) — chain-head publication" section, which records the two rejected alternatives and the InnoDB / PostgreSQL / Memgraph revisions read; design in `04-technical-design.md` §5.7.1), and `D-wal-group-leader`, **ratified 2026-08-11** (how the WAL is hardened once a database engine runs more than one worker, rmp #1040: one fsync group leader per database engine replaces the per-worker fsync thread, so at most one `fdatasync` is in flight system-wide and a newer offer subsumes and discards an older pending one, releasing every worker whose target it passed — the `XLogFlush` shape of PostgreSQL; the strict depth-1 invariant of the pipelined group commit of rmp #532, false the moment the engine grew to `W` workers, is restated as a property of the database rather than of a worker; and the three previously undeclared properties the coalescing rests on are promoted to a normative, test-pinned **sink contract**, with every discard adjudicated by an always-on tripwire — see the "Ratified decision (2026-08-11) — the WAL fsync group leader" section; design in `04-technical-design.md` §4.2 and §9.1), and `D-published-snapshot-horizon`, **ratified 2026-08-12** (what a snapshot timestamp means once more than one worker may be committing, rmp #1056: a snapshot is the **published commit-visibility horizon** and never the commit-timestamp allocation clock, because a commit publishes itself in two writes in two media *after* its timestamp is issued, and handing out the issuing clock let a second worker begin at a timestamp naming a commit no oracle would yet admit — a **lost update** at eight engine workers and never at one; the horizon is the contiguous published prefix of the issued-but-unpublished set, guarded by the rank-20 commit sequencer latch, monotone, and off the read path, with every issued timestamp released on **every** exit because a leaked one freezes the horizon permanently, and with `begin` and the commit path returning the timestamp they captured or assigned instead of re-reading a shared clock — see the "Ratified decision (2026-08-12) — the published snapshot horizon" section, which records the Memgraph and PostgreSQL revisions read and the `SsiTracker::are_concurrent` premise that was suspected and **refuted**; design in `04-technical-design.md` §5.2, §5.1.3 and §5.7). Also records a **post-ratification note dated 2026-08-12** on `D-write-conflict-detection` (task #1056 implemented the ratified rule's **second arm** — a chain head whose transaction committed after the writer's start timestamp is now refused — and fixes the reading of its boundary: the ratified "committed before its own start timestamp" is evaluated as **at or before**, because a begin timestamp *is* the visibility horizon and a strict comparison would abort the ordinary sequential case), and a **post-ratification note dated 2026-08-05** on `D-dst-writer-scheduler` (rmp #973 delivered the deterministic thread scheduler, so the decision is **partly discharged** — the concurrent writers it is to order arrive with rmp #975; the note also records the three implementation choices that constrain future work: a cargo feature rather than `debug_assertions`, a purpose-built scheduler rather than `loom` or `shuttle`, and the invariant that the execution token is only handed over where no page latch and no page-table shard lock is held — see the "Post-ratification note (2026-08-05) — `D-dst-writer-scheduler`" section; design in `07-dst-simulator.md` §5.2), and `D-page-write-order`, **ratified 2026-08-12** (what a logged page write must guarantee once more than one worker may be writing, rmp #1062: **for every page, WAL log order is the order in which records take effect on that page** — a logged page write is two instants in two media, the record entering the log under the WAL mutex and the change taking effect on the page under the frame latch, and until #1062 nothing tied them together, so with several writers the durable state after a crash **differed** from the state a crash-free execution left, measured as 32 page-LSN descents across sixteen deterministic seeds with **not one of them on a chain head**; the invariant is supplied by the rank-27 section **re-keyed from `(store kind, record id)` to the device page**, with the page pinned before the latch, making "capture the undo pre-image, append, apply, stamp `page_lsn`" one indivisible section per page behind a single logging door; three alternatives are rejected — the monotone `page_lsn` stamp already present since rmp #1029 and empirically insufficient, and worse than useless alone because a page stamped with an LSN it never applied makes redo **skip** a legitimate record; appending under the frame latch, which inverts ranks 30 and 40; and applying under the WAL mutex, which violates the absolute no-WAL-lock-across-a-pool-call rule — see the "Ratified decision (2026-08-12) — log order equals apply order, per page" section, which also records the honest limitation that the B+-tree index layer's eight WAL-append sites satisfy the invariant by Rust exclusivity rather than by this mechanism; design in `04-technical-design.md` §5.7.2 and §3.3). Also records a **post-ratification note dated 2026-08-12** on `D-chain-head-publication` (task #1062 re-keyed its rank-27 latch from `(store kind, record id)` to the device page; the widening is **strict**, so its ratified guarantee is unchanged, and the decision is now a particular case of `D-page-write-order` — both keep their own key and date because each names a real and distinct scope). Also records two **post-ratification notes dated 2026-08-13** (task #1069): on `D-version-representation` (the commit indirection point now covers the **record header** too — an unsettled `created_ts` / `expired_ts` names the writer's `commit.store` slot instead of its `TxnId`, so the store has one commit oracle instead of two; no byte moved, and the on-disk format version went 5 → 6 because the payload's *meaning* did; the note places the change against the ratified Memgraph / InnoDB citation, read from both source trees, and records the silent loss of committed data on a backup-chain restore that it closes), and on `D-orphan-slot-parking` (the commit-info slot is now parked by the same mechanism, for the sharper reason that recycling one re-attributes a committed version to a different transaction, and for a commit slot the parking is the only route to the free list rather than a restraint on the abort path). |
| `03-sources.md` | Authoritative references (standards, papers, vendor docs) backing every requirement. |
| `04-technical-design.md` | The implementation-design layer (the "HOW"): crate boundaries, on-disk layouts, byte-level formats, algorithms, and control/data flow. Collects the remaining measurement-gated spikes in §12. §6.7 documents the full-text index (advanced; delivered ahead of Phase 2, rmp #72). §6.8 specifies named node-property indexes and the Neo4j-conformant index DDL (`CREATE`/`DROP INDEX`, `SHOW INDEXES`; decision `D-named-index-autoname`, rmp #623–#626). §7.8 specifies the `EXPLAIN`/`PROFILE` query prefixes and the measured-`dbHits` model (decision `D-query-prefixes`, rmp #752). §9.5 specifies hardware-aware startup auto-tuning (decision `D-hw-autotune`). §5.1 specifies the ratified MVCC version representation — the delta actions, the delta lifecycle and its ownership by the transaction, the commit indirection point, `command_id`, and the five mechanisms the unified chain replaced together with the task that retired each (all five are now closed); §5.6 the interaction with the record store and indexes; §5.7 latching, header-based conflict detection and multi-writer execution (decisions `D-version-representation`, `D-write-conflict-detection`, `D-multi-writer`, `D-dst-writer-scheduler`). The property path onto that chain is specified by the 2026-08-03 decisions: §5.1.2 step 1 fixes when the entity-granularity conflict check arrives and why it is a prerequisite of the migration; §5.1.5 "row 1 in detail" specifies the empty-cell representation of a property removal and the one-owner-per-overflow-chain rule; §5.6 makes the undo chain the sole visibility oracle for a property's value; §11.6 states how "all existing tests stay green" is read when a task retires a mechanism (`D-property-write-conflict`, `D-property-removal`, `D-property-visibility`, `D-retired-mechanism-tests`). §5.1.4 specifies statement-level isolation as delivered by task #972 (`D-statement-isolation`): the `New`/`Old` view rule and the `command_id = 0` baseline carve-out, where the counter lives and who may stamp a delta with it, the two points at which it advances, the per-clause polarity table, and why the planner's `Eager` barriers are all retained; §5.3 divides the cross-transaction visibility predicate from the chain-resolved, statement-granular refinement; §5.1.5 "row 2, second consequence" records the label creator gate narrowed to the creating statement. §5.7.1 specifies the **chain-head prepend publication protocol** as delivered by task #1028 (`D-chain-head-publication`): the four ordered steps and why fixing the displaced back-pointer must follow the winning publication, the retry a refusal demands, the two ordering obligations the medium must honour (atomicity and durable log order), the sharded rank-27 publication latch that supplies them, the rule that a refused publication logs nothing, the two writes beside the head word the same latch covers, and — stated explicitly rather than left to inference — the *unlink* paths that still install a chain head with an unconditional whole-record write and are therefore outstanding; §3.3 states the engine's **latch ranks** and the two obligations ranks 25 and 27 carry; §4.4 specifies the conditional (compare-and-set) redo image; §5.1.2 step 3 is the delta-linking step that consumes the protocol; §11.4 states why a model-checkable protocol must live in a crate with no edge to `graphus-bufpool`. §4.2 specifies the **group-commit durability path** as delivered by tasks #532 and #1040 (`D-wal-group-leader`): the pipelined harden that overlaps batch *K*'s `fdatasync` with batch *K+1*'s preparation, the one fsync group leader per database engine that replaced the per-worker fsync thread, the restatement of depth-1 as "at most one `fdatasync` in flight per database", and the three normative sink-contract properties the coalescing rests on together with the always-on tripwire that adjudicates every discard; §9.1 places that leader off the async runtime. §5.2 specifies the **published snapshot horizon** as delivered by task #1056 (`D-published-snapshot-horizon`): the normative snapshot invariant, why the commit-timestamp allocation clock is not a snapshot, the contiguous-published-prefix horizon and its monotone publication, the rank-20 commit sequencer latch with its single acquisition door, the obligation to release an issued timestamp on every exit (and the permanent horizon freeze a leaked one causes), the rule that `begin` and the commit path return their own timestamp rather than re-reading a shared clock, and the naming boundary against the commit ordering of task #977; §5.1.3 states what the commit-info slot's single store does and does not settle; §5.7 states the second arm of the write-conflict check and why its boundary is "at or before"; §9.2 separates reading the horizon (lock-free) from issuing a commit timestamp (under the sequencer latch). §5.7.2 specifies **log order is apply order, per page** as delivered by task #1062 (`D-page-write-order`): the normative per-page ordering invariant and its equivalent recovery form, why the two instants of a logged page write are not coupled for free, the page-LSN descent as the observable signature, why a monotone `page_lsn` is not the fix (and why relying on it alone makes redo skip a legitimate record), the four ordered steps of the rank-27 section keyed by the device page and why the undo pre-image capture must be inside it, the single logging door and the ten append sites plus the rollback compensation that pass through it, why the key is the page and not the record, the two rejected routes (rank inversion 30/40; the WAL lock across a pool call), the contention cost accepted, the deterministic evidence with its non-vacuity controls and ablation, and the explicit limitation that the B+-tree index layer's eight WAL-append sites satisfy the invariant by Rust exclusivity rather than by this mechanism; §3.3 carries the rank-27 latch's renaming to the page log-apply-order latch and adds rank 5 to the rank list; §5.7.1 records that its own latch key was widened and that the protocol it specifies is otherwise unchanged. §5.3 further specifies the **single commit oracle** as delivered by task #1069: an unsettled record stamp names the writer's commit slot rather than its `TxnId`, `graphus_txn::is_visible_via` replaced the removed free function `is_visible`, resolving a stamp reads a page and therefore fails closed, and the freeze sweep, its frontier and the WAL reclamation floor survive as performance rather than correctness (task #1070 removes them); §5.1.3 and §5.6 state that a commit-info slot is reclaimed by a **census of references** — live deltas, in-use record headers and open transactions — and is parked for one collection pass before its id returns to the allocator. |
| `05-storage-format.md` | Phase 1 spike: storage-format and durability micro-decisions. Freezes the page header, the versioned-record header, the record-store and B+-tree page layouts, and the offline backup artifact. §12 freezes the **undo area** — the delta record, the commit-info slot, their durability and recovery rules, and the on-disk format-version bump (`D-version-representation`, task #966); §7 defines the meaning of `undo_ptr`, records that task #966 brought the field to life, and specifies how the shared 25-byte MVCC header reads on a **property cell** from task #967 onward (`created_ts` informative only; `expired_ts` never written by a property operation). §12.2 carries two clarifications, both prose filling a gap with **no byte of the frozen layout changing**: the 2026-08-03 one, that `type_tag == 0` on a `SetProperty` delta means the property did not exist before; and the 2026-08-05 one (task #972, `D-statement-isolation`), that `command_id == 0` means the write happened outside any statement and belongs to the transaction's baseline, and that the field is written from the transaction's own counter and never by the caller. §12.5 records how a live abort and a crashed loser differ once rollback is logical (task #970): the transaction applies its own deltas, while recovery still undoes a loser from the WAL. Task #1028 (`D-chain-head-publication`) added **no byte change** and is recorded as such: §7 states that a prepend now installs a chain head by compare-and-publish, §9 that a chain-head write's redo patch is itself a compare-and-set image reusing the existing patch encoding, and §12.5 that a refused publication left nothing in the log to replay. Task #1069 is recorded the same way — **no byte moved**: §7 specifies the **header stamp** and why an unsettled one names a `commit.store` slot rather than a `TxnId`; §11 states that a restored image carries its own commit oracle and that the pre-capture header freeze of task #149 is retired; §12.4 replaces slot reclamation by delta count with a reference census plus parking; §12.5 records what the consistency checker still proves and the one obligation it does not yet check; and §12.6 documents format versions 5 and 6, version 6 being conditionally refused with a migration route. |
| `06-bolt-and-error-shapes.md` | Phase 1 spike: pins the Bolt version (5.4), freezes the TCK error-classification table and the Bolt/REST result and failure shapes, and specifies the REST transactional access-mode field. Closes decision-register open questions 2 and 5. The result-summary shape (§3.1) includes the `EXPLAIN`/`PROFILE` execution-plan metadata (`plan`/`profile`; rmp #752). |
| `07-dst-simulator.md` | The external **deterministic simulator** (VOPR, decision `D-vopr`): determinism model, architecture, the three real wire clients, workloads, the cooperative interleaver of overlapping transactions, the seeded fault models (disk, clock, transport, crash + ARIES) and unified fault scheduler, the live-device `dst` fault seam, the oracles (incl. the strong reference model and the Elle isolation checker), the safety/liveness/swarm certification modes, replay artifacts + config shrinker, the hyper-speed fuzzer, the CLI and CI gates, the scenario catalogue, and the engine gaps it surfaced (`rmp` #171/#172/#220). §5.1 names the **concurrency-fidelity ceiling** — what the simulator does not cover and which suite owns each item — and §5.2 specifies the **deterministic thread scheduler** (`D-dst-writer-scheduler`, rmp #973): one execution token handed between real OS threads at declared yield points with a seeded successor draw, the invariant that fixes where the token may change hands, the byte-comparable scheduling history, the installed yield points, and the `det-sched` gating that keeps its production cost at zero. The scheduler **narrows** the ceiling — garbage collection racing an off-thread reader at page-latch granularity is now seed-reproducible (`rmp` #811) — without removing it: N parallel writers are **not** certified, and the memory model remains owned by the `loom` and ThreadSanitizer lanes, with which the scheduler is mutually exclusive at compile time. §5.1 also records that two writers prepending onto one chain head remain outside the ceiling, owned by the `graphus-chainhead` `loom` model (rmp #1028); §6.2 adds the `dst`-gated chain-head publication seam that lets a scenario script that interleaving instead of racing it, and §10 catalogues the crash-recovery scenario built on it. |
| `08-network-bulk-import.md` | **Ratified 2026-07-01.** Network bulk import: streaming a large dataset over the network into an already-running Graphus server, via a dedicated REST streaming-upload endpoint. Two ratified modes: Mode A (fresh/empty database, exclusive `Loading` state) and Mode B (already-live database, fully concurrent — every batch is a first-class, SSI-participating transaction through the same write path as ordinary Cypher writes, so it never violates serializability). Covers payload format, RBAC gating (global `Admin`), ACID/resumability semantics (extends `D-bulk-import-non-atomic`), a prerequisite `graphus-bulk` correctness fix for safe batch retry, and DoS/resource limits. Registers decision `D-bulk-import-network` (`02-decision-register.md`) and `FR-BK-7` (`01-needs-survey.md`). |

## How to read this

1. Start with `00-overview.md` for the project frame and the four inviolable requirements.
2. Read `01-needs-survey.md` for the complete enumeration of what Graphus must do.
3. Review `02-decision-register.md` and ratify each decision; ratified decisions unblock
   the detailed, per-domain functional specification (one document per domain) and the
   implementation sprints in `rmp`.

## Requirement identifiers

Functional needs are identified as `FR-<DOMAIN>-<n>` (e.g. `FR-DM-1`).
Non-functional requirements are `NFR-<n>`. Decisions are `D-<slug>`.
These identifiers are stable and used for traceability across `rmp` tasks and the
Knowledge Graph.

## Status

- **Stage:** Specify (baseline complete; all 24 baseline design decisions ratified, plus the
  post-ratification `D-bulk-import-network`, ratified 2026-07-01 — see
  `08-network-bulk-import.md`; `D-hw-autotune`, ratified 2026-07-08 — see
  `04-technical-design.md` §9.5; the four MVCC-native-engine decisions ratified 2026-08-02 —
  see `04-technical-design.md` §§5.1, 5.6, 5.7 and `05-storage-format.md` §12; the four
  property-path decisions ratified 2026-08-03 — see `04-technical-design.md` §§5.1.2, 5.1.5, 5.6,
  11.6 and `05-storage-format.md` §§7, 12.2; `D-incidence-anchor`, ratified 2026-08-04 — see
  `04-technical-design.md` §5.1.1; the three logical-rollback decisions ratified 2026-08-05 — see
  `04-technical-design.md` §§4.3, 4.4, 5.1.2, 5.1.5 and `05-storage-format.md` §§7, 9, 12.3, 12.5;
  `D-statement-isolation`, also ratified 2026-08-05 — see `04-technical-design.md` §§5.1.4, 5.3
  and `05-storage-format.md` §12.2; `D-chain-head-publication`, ratified 2026-08-09 — see
  `04-technical-design.md` §5.7.1; `D-wal-group-leader`, ratified 2026-08-11 — see
  `04-technical-design.md` §4.2 and §9.1; `D-published-snapshot-horizon`, ratified 2026-08-12 —
  see `04-technical-design.md` §5.2; and `D-page-write-order`, also ratified 2026-08-12 — see
  `04-technical-design.md` §5.7.2 and §3.3). Two post-ratification notes dated 2026-08-13 record
  task #1069 against `D-version-representation` and `D-orphan-slot-parking` — see
  `04-technical-design.md` §5.3 and `05-storage-format.md` §7.
- **Next:** per-domain detailed functional specification (one document per domain), consistent
  with the ratified decisions; then Phase 1 implementation planning in `rmp`. Network bulk import
  (`FR-BK-7`) is unblocked for `rmp` implementation planning; Mode B's automatic batch retry has
  one prerequisite `graphus-bulk` fix (`08-network-bulk-import.md` §7.2.2) that must be sequenced
  first.
- **Roadmap / KG:** `rmp` roadmap `graphus` (the queryable project map; updated on every commit).
- **Inviolable requirements:** 100% ACID compliant, 100% Cypher TCK compliant,
  100% Bolt protocol compliant, 100% PackStream compliant.
- **Interfaces:** Bolt over UDS, Bolt over TCP, and the Web REST API (three; the Bolt-over-TCP
  interface is an owner-ratified extension of the project definition).
