{
  "version": 2,
  "metadata": {
    "scenario": "durability-crash-recovery",
    "description": "Deterministic OLTP durability + crash-recovery under load, driven by the DST simulator: a concurrent overlapping-transaction workload under disk/clock faults and a seeded mid-workload crash, rebuilt via ARIES recovery, with the four ACID-durability properties (serializability / durability / atomicity / reference-model equivalence) asserted on the recovered engine against the committed-only shadow LPG.",
    "dataset": {
      "nodes": 44,
      "relationships": 9
    },
    "workload": {
      "clients": "6 (overlapping explicit transactions)",
      "crashes_per_seed": "<=2 (mid-workload crash + ARIES restart)",
      "driver": "graphus-dst VOPR safety oracle (run_safety)",
      "focus_recovered_txns": "24",
      "focus_recovery_records_replayed": "16",
      "focus_seed": "7",
      "recovery_crashes": "60",
      "recovery_inflight_undone": "255",
      "recovery_records_replayed": "401",
      "scenario": "oltp-durability",
      "seeds": "1..31"
    },
    "started_unix_secs": 1783751168
  },
  "host": {
    "os": "linux",
    "arch": "x86_64",
    "cpu_cores": 16,
    "hostname": "ROG",
    "rustc_version": "rustc 1.96.0 (ac68faa20 2026-05-25)",
    "timestamp_unix_secs": 1783751168
  },
  "total_millis": 369.557931,
  "phases": [
    {
      "name": "durability sweep (workload + crash + ARIES recovery + 4-property oracle)",
      "millis": 369.557931
    }
  ],
  "cpu": {
    "user_secs": 0.334901,
    "system_secs": 0.043116,
    "mean_core_utilisation": 1.0074649985687043
  },
  "memory": {
    "peak_rss_bytes": 19017728,
    "final_rss_bytes": 19120128
  },
  "storage": {
    "store_bytes": 0,
    "wal_bytes": 0,
    "store_pages": 0,
    "wal_pages": 0,
    "bytes_fsynced": 0,
    "write_amplification": 0.0,
    "space_amplification": 0.0
  },
  "throughput": {
    "operations": 30,
    "ops_per_sec": 81.17807110463555,
    "p50_latency_ms": 0.0,
    "p99_latency_ms": 0.0,
    "p999_latency_ms": 0.0,
    "abort_rate": 0.0
  },
  "measurement_mode": "local",
  "notes": [
    "durability oracle: 30 seed(s) checked, 0 unsafe, 0 non-deterministic — properties: [\"serializability\", \"durability\", \"atomicity\", \"reference-model-equivalence\"]",
    "crash + ARIES restarts across sweep: 60; faults injected: 240; acked commits proven durable: 401; in-flight transactions discarded by undo: 255; non-vacuous runs (both halves of committed-or-nothing under test): 30/30",
    "recovered dataset of the focus seed (MEASURED, rmp #698): 44 :Person nodes and 9 :KNOWS relationships, read back from the RECOVERED engine and equal to the committed-only shadow model (44 nodes / 9 edges). The relationship count was previously reported as 0.",
    "mean cost of one full crash-recovery scenario: 12.3 ms (30 seeds in 369.6 ms). Per-seed latency percentiles are NOT measured by this hermetic sweep and are therefore left at their 'not measured' default rather than fabricated (rmp #699).",
    "storage is 0 here BY CONSTRUCTION, not by omission: the DST core runs the engine on an in-memory device + in-memory WAL sink, so there is no on-disk footprint to size. The real on-disk store/WAL footprint (and the redo log a crash leaves behind) is measured by the sibling real-server SIGKILL run, in evidence/real-server/report.json.",
    "the FULL fault catalogue (crash steal/no-force, torn WAL tail, torn data page + doublewrite repair, write reordering, write I/O error) is driven by the sibling `durability_faults` binary, which this example's run.sh asserts SAFE — this sweep is the crash-and-redo core, not the whole fault surface.",
    "deterministic-vs-machine-variant split: the recovery-work counts (recovery_records_replayed / recovery_inflight_undone / recovery_crashes), the durability verdict, the recovered hashes and the recovered dataset size are EXACTLY reproducible (a pure function of the seed range) and are what the committed baseline gates; the sweep wall-time, the seed-rate throughput and this process's CPU/RSS are machine-variant and are NOT gated."
  ]
}